name: Trigger auto deployment for open-lovable

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/open-lovable-AutoDeployTrigger-eceaffad-d2ca-43a2-8335-2c5bea7b9d05.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.OPENLOVABLE_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.OPENLOVABLE_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.OPENLOVABLE_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: shopish.azurecr.io
          registryUsername: ${{ secrets.OPENLOVABLE_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.OPENLOVABLE_REGISTRY_PASSWORD }}
          containerAppName: open-lovable
          resourceGroup: Shopish
          imageToBuild: shopish.azurecr.io/open-lovable:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


